const path = require("path");

const filePath = path.resolve(__dirname);
const dirPath = path.resolve("./app-5.js");

console.log(`The File Path is ${filePath}`);
console.log(`The Directory Path is ${dirPath}`);